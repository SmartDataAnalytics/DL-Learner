<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">


    <!--We inherit from a parent pom, this way all of our modules can have consistent dependencies, plugins, etc
        Our artifact inherits the groupId and version.-->
    <parent>
        <artifactId>counsel-parent</artifactId>
        <groupId>com.issinc.counsel</groupId>
        <version>0.1-SNAPSHOT</version>
    </parent>

    <!--Standard Version required by Maven-->
    <modelVersion>4.0.0</modelVersion>

    <!--Artifact Id-->
    <artifactId>dl-learner</artifactId>
    <build>
        <plugins>

            <!--Maven Compiler Plugin-->
            <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>2.0.2</version>
                    <configuration>
                        <!--We use 1.6-->
                        <source>${compiler.version}</source>
                        <target>${compiler.version}</target>
                        <encoding>${project.build.sourceEncoding}</encoding>
                    </configuration>
                </plugin>
            <!--JAR the Sources-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!--JAR the Java docs-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!--Surefire - for JUnits-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>2.4.3</version>
                <configuration>
                    <suiteXmlFiles>
                        <suiteXmlFile>src/test/testng.xml</suiteXmlFile>
                    </suiteXmlFiles>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <dependencies>

        <dependency>
            <groupId>net.sourceforge.owlapi</groupId>
            <artifactId>owlapi</artifactId>
            <version>3.0.0</version>
        </dependency>
        
        <!--Hermit is not available via Central-->
        <dependency>
            <groupId>org.semanticweb.hermit</groupId>
            <artifactId>hermit</artifactId>
            <version>1.2.3</version>
        </dependency>

        <!--Available via central, we use the latest with minor mods to DL Learner source (IE Dig related code)-->
        <dependency>
            <groupId>com.hp.hpl.jena</groupId>
            <artifactId>jena</artifactId>
            <version>2.6.2</version>
        </dependency>

        <!--Ignore this dependency, it's for our modifications to DL Learner.-->
        <!--<dependency>-->
            <!--<groupId>org.springframework</groupId>-->
            <!--<artifactId>org.springframework.core</artifactId>-->
        <!--</dependency>-->
        
        <!--These are the protege dependencies from DL Learner distribution-->
        <dependency>
            <groupId>org.protege.editor.core</groupId>
            <artifactId>org.protege.editor.core.application</artifactId>
            <version>4.1.0.206</version>
        </dependency>
        <dependency>
            <groupId>org.protege.editor.owl</groupId>
            <artifactId>org.protege.editor.owl</artifactId>
            <version>4.1.0.206</version>
        </dependency>
        <dependency>
            <groupId>org.protege.common</groupId>
            <artifactId>org.protege.common</artifactId>
            <version>4.1.0.206</version>
        </dependency>


        <!--Had to construct this from pellet release files -->
        <dependency>
            <groupId>com.owldl</groupId>
            <artifactId>pellet</artifactId>
            <version>2.1.1</version>
        </dependency>

        <!--Another not in central-->
        <dependency>
            <groupId>net.sourceforge.owlapi.owllink</groupId>
            <artifactId>owllink</artifactId>
            <version>1.0.1</version>
        </dependency>
        <!--JSON is in Central-->
        <dependency>
            <groupId>org.json</groupId>
            <artifactId>json</artifactId>
            <version>20090211</version>
        </dependency>


        <!--JAMON is in Central-->
        <dependency>
            <groupId>com.jamonapi</groupId>
            <artifactId>jamon</artifactId>
            <version>2.7</version>
        </dependency>


        <!--SwingX is in central-->
        <dependency>
            <groupId>org.swinglabs</groupId>
            <artifactId>swingx</artifactId>
            <version>1.6</version>
        </dependency>

        <!--JENA ARQ is in central - we use the latest-->
        <dependency>
            <groupId>com.hp.hpl.jena</groupId>
            <artifactId>arq</artifactId>
            <version>2.8.2</version>
        </dependency>

        <!--These are the ORE dependencies -->
        <dependency>
            <groupId>net.sourceforge.browserlauncher2</groupId>
            <artifactId>browserlauncher2</artifactId>
            <version>1.3</version>
            <!--This version is the one released with DL-Learner 2048-->
        </dependency>

        <dependency>
            <groupId>com.jgoodies</groupId>
            <artifactId>looks</artifactId>
            <version>2.3.0</version>
            <!--This version is the one released with DL-Learner 2048-->
        </dependency>

        <dependency>
            <groupId>com.google.code.factplusplus</groupId>
            <artifactId>factpp-owlapi</artifactId>
            <version>1.4.0.1</version>
            <!--This version is the one released with DL-Learner 2048-->
        </dependency>

        <!--Lucene is in Central-->
        <dependency>
            <groupId>org.apache.lucene</groupId>
            <artifactId>lucene-core</artifactId>
            <version>3.0.1</version>
        </dependency>

        <dependency>
            <groupId>org.ini4j</groupId>
            <artifactId>ini4j</artifactId>
            <version>0.3.2</version>
            <!--This version is the one released with DL-Learner 2048-->
        </dependency>

        <dependency>
            <groupId>net.sourceforge.dig</groupId>
            <artifactId>dig-xmlbeans</artifactId>
            <version>1.1</version>
            <!--This version is the one released with DL-Learner 2048-->
        </dependency>

        <dependency>
            <groupId>xmlbeans</groupId>
            <artifactId>xbean</artifactId>
            <version>2.2.0</version>
        </dependency>

        <dependency>
            <groupId>net.sourceforge.secondstring</groupId>
            <artifactId>secondstring</artifactId>
            <version>20060615</version>
            <!--This version is the one released with DL-Learner 2048-->
        </dependency>

        <dependency>
            <groupId>net.sf.jopt-simple</groupId>
            <artifactId>jopt-simple</artifactId>
            <version>3.1</version>
            <!--This version is the one released with DL-Learner 2048-->
        </dependency>

    </dependencies>


</project>
